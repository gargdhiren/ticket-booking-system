server:
  port: 8080

spring:
  application:
    name: backend

  datasource:
    url: jdbc:postgresql://localhost:5432/ticket_booking
    username: postgres
    password: mysecretpassword

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true